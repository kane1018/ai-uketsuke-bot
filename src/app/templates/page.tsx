import Link from "next/link";
import { MarketingShell, TrialCta } from "@/components/MarketingShell";
import { pageMetadata } from "@/lib/site";
import { USE_CASES, signupPath } from "@/lib/use-cases";
export const metadata=pageMetadata("業種別の受付フォーム・質問テンプレート", "士業の相談、不動産の問い合わせ、採用応募。受付Botで作る会話形式のフォームの使い方と質問例、できること・できないことをご紹介。", "/templates");
export default function TemplatesPage(){return <MarketingShell><section className="section-wrap"><div className="section-heading"><p className="eyebrow">業務に合う入口を選ぶ</p><h1>最初に何を聞くか。<br/>テンプレートから考える。</h1><p>完成形を一から考えなくても、用途と業種から始められます。必要な質問だけを残して使ってください。</p></div><div className="grid gap-6 md:grid-cols-3">{USE_CASES.map((item)=><article className="card flex flex-col p-6" key={item.slug}><p className="text-xs text-slate-500">{item.audience}</p><h2 className="mt-3 text-xl font-bold">{item.name}</h2><p className="mt-3 text-sm leading-7 text-slate-600">{item.description}</p><ul className="my-5 flex-1 space-y-2 text-sm text-slate-700">{item.examples.map((q)=><li key={q}>✓ {q}</li>)}</ul><Link href={`/templates/${item.slug}`} className="btn-secondary w-full">質問例と使い方を見る</Link></article>)}</div><div className="mt-8 rounded-2xl bg-slate-50 p-6"><h2 className="text-lg font-bold">上の業種以外でも使えます</h2><p className="mt-2 text-sm leading-7 text-slate-600">作成画面では「その他」を選び、問い合わせ・相談・事前ヒアリングなどの目的に合わせて質問を編集できます。自由回答を自動生成するAIではなく、保存した質問に沿って受付する仕組みです。</p><Link href={signupPath()} className="mt-4 inline-block font-semibold text-brand-700 underline">自分で質問を選んで作る →</Link></div></section><TrialCta /></MarketingShell>;}
