# Supabase
.branches
.temp

# Local CLI configuration is not a production source of truth. Hosted project
# configuration is managed separately; never push a freshly initialized file.
config.toml

# dotenvx
.env.keys
.env.local
.env.*.local
